# --IMDB-Movie-Review-Sentiment-Analysis
Built and evaluated multiple NLP models (Naive Bayes, Logistic Regression, Linear SVM, and ensembles) using TF-IDF features to classify movie reviews as positive or negative. Performed model comparison, hyperparameter tuning, and validation, achieving strong performance (~85–90% accuracy) while analyzing overfitting and generalization.
